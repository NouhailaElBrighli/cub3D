#include <stdio.h>
#include<stdlib.h>

int main()
{
	// char *s;

	// s = malloc(sizeof(char)* 5);
	// printf("address == %p\n", s);
	// free(s);
	// s = malloc(sizeof(char)* 10);
	// printf("address == %p\n", s);

}